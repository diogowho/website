---
import Layout from "../layouts/Default.astro";

const servers = [
  {
    name: "lotus",
    provider: "OVH",
    location: "Gravelines, FR",
    hostname: "gra.diogocastro.net",
  },
  {
    name: "dahlia",
    provider: "UpCloud",
    location: "Helsinki, FI",
    hostname: "hel.diogocastro.net",
  },
  {
    name: "poppy",
    provider: "Myself",
    location: "Setúbal, PT"
  }
];
---

<Layout title="Infrastructure" showBackLink={true}>
  <section class="mb-8">
        <h2 class="mb-2 text-base font-bold text-[#888888]">What's this?</h2>
        <p class="mb-4">
          Coming from my love for networking and servers, I decided to start hosting my own services and websites.
        </p>
        <p class="mb-4">
          With time, I plan to expand my infrastructure, host more services, learn new things, and share it with friends.
        </p>
      </section>

      <section class="mb-8">
        <h2 class="mb-2 text-base font-bold text-[#888888]">
          Servers
        </h2>
        <ul class="m-0 list-none p-0">
          {servers.map((server) => (
            <li class="mb-4 before:text-[#888888] before:content-['>_']">
              {server.name} 
              <div class="pl-5 text-[#888888]">
                <div>{server.provider}, {server.location}</div>
                {server.hostname && <div>{server.hostname}</div>}
              </div>
            </li>
          ))}
    </ul>
  </section>
</Layout>
